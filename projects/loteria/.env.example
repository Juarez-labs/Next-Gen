# Copy to `.env` and fill in. Only the EXPO_PUBLIC_* vars are bundled into the app.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (set as Supabase Edge Function secrets, never in the app bundle):
# OPENAI_API_KEY=
